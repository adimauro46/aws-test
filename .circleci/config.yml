# This config was automatically generated from your source code
# Stacks detected: deps:java:microservices/product-service,tool:gradle:
version: 2.1
jobs:
  build-eureka-server:
    docker:
      - image: cimg/openjdk:17.0
    working_directory: ~/project/microservices/eureka-server
    steps:
      - checkout:
          path: ~/project
      - setup_remote_docker:
          version: default
      - run:
          name: Calculate cache key
          command: |-
            find . -name 'pom.xml' -o -name 'gradlew*' -o -name '*.gradle*' | \
                    sort | xargs cat > /tmp/CIRCLECI_CACHE_KEY
      - restore_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
      - run:
          name: set gradle
          command: sudo chmod +x ./gradlew
      - run:
          name: Build
          command: ./gradlew bootJar
      - save_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
          paths:
            - ~/.gradle/caches
      - run:
          name: Build Docker Image
          command: |
            docker build -t alfiodimauro/adm_solutions:eureka-server .
      - run:
          name: Push Docker Image
          command: |
            docker login -u alfiodimauro -p Alf10@1978!!
            docker push alfiodimauro/adm_solutions:eureka-server
  build-order-service:
    docker:
      - image: cimg/openjdk:17.0
    working_directory: ~/project/microservices/order-service
    steps:
      - checkout:
          path: ~/project
      - setup_remote_docker:
          version: default
      - run:
          name: Calculate cache key
          command: |-
            find . -name 'pom.xml' -o -name 'gradlew*' -o -name '*.gradle*' | \
                    sort | xargs cat > /tmp/CIRCLECI_CACHE_KEY
      - restore_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
      - run:
          name: set gradle
          command: sudo chmod +x ./gradlew
      - run:
          name: Build
          command: ./gradlew bootJar
      - save_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
          paths:
            - ~/.gradle/caches
      - run:
          name: Build Docker Image
          command: |
            docker build -t alfiodimauro/adm_solutions:order-service .
      - run:
          name: Push Docker Image
          command: |
            docker login -u alfiodimauro -p Alf10@1978!!
            docker push alfiodimauro/adm_solutions:order-service          
  build-product-service:
    docker:
      - image: cimg/openjdk:17.0
    working_directory: ~/project/microservices/product-service
    steps:
      - checkout:
          path: ~/project
      - setup_remote_docker:
          version: default
      - run:
          name: Calculate cache key
          command: |-
            find . -name 'pom.xml' -o -name 'gradlew*' -o -name '*.gradle*' | \
                    sort | xargs cat > /tmp/CIRCLECI_CACHE_KEY
      - restore_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
      - run:
          name: set gradle
          command: sudo chmod +x ./gradlew
      - run:
          name: Build
          command: ./gradlew bootJar
      - save_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
          paths:
            - ~/.gradle/caches
      - run:
          name: Build Docker Image
          command: |
            docker build -t alfiodimauro/adm_solutions:product-service .
      - run:
          name: Push Docker Image
          command: |
            docker login -u alfiodimauro -p Alf10@1978!!
            docker push alfiodimauro/adm_solutions:product-service
  build-inventory-service:
    docker:
      - image: cimg/openjdk:17.0
    working_directory: ~/project/microservices/inventory-service
    steps:
      - checkout:
          path: ~/project
      - setup_remote_docker:
          version: default
      - run:
          name: Calculate cache key
          command: |-
            find . -name 'pom.xml' -o -name 'gradlew*' -o -name '*.gradle*' | \
                    sort | xargs cat > /tmp/CIRCLECI_CACHE_KEY
      - restore_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
      - run:
          name: set gradle
          command: sudo chmod +x ./gradlew
      - run:
          name: Build
          command: ./gradlew bootJar
      - save_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
          paths:
            - ~/.gradle/caches
      - run:
          name: Build Docker Image
          command: |
            docker build -t alfiodimauro/adm_solutions:inventory-service .
      - run:
          name: Push Docker Image
          command: |
            docker login -u alfiodimauro -p Alf10@1978!!
            docker push alfiodimauro/adm_solutions:inventory-service
  build-api-gateway:
    docker:
      - image: cimg/openjdk:17.0
    working_directory: ~/project/microservices/api-gateway
    steps:
      - checkout:
          path: ~/project
      - setup_remote_docker:
          version: default
      - run:
          name: Calculate cache key
          command: |-
            find . -name 'pom.xml' -o -name 'gradlew*' -o -name '*.gradle*' | \
                    sort | xargs cat > /tmp/CIRCLECI_CACHE_KEY
      - restore_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
      - run:
          name: set gradle
          command: sudo chmod +x ./gradlew
      - run:
          name: Build
          command: ./gradlew bootJar
      - save_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
          paths:
            - ~/.gradle/caches
      - run:
          name: Build Docker Image
          command: |
            docker build -t alfiodimauro/adm_solutions:api-gateway .
      - run:
          name: Push Docker Image
          command: |
            docker login -u alfiodimauro -p Alf10@1978!!
            docker push alfiodimauro/adm_solutions:api-gateway
  deploy:
    docker:
      - image: circleci/python:3.8
    steps:
      - checkout
      - setup_remote_docker:
          version: default
      - run:
          name: Install kubectl
          command: |
            curl -LO "https://storage.googleapis.com/kubernetes-release/release/$(curl -s https://storage.googleapis.com/kubernetes-release/release/stable.txt)/bin/linux/amd64/kubectl"
            chmod +x ./kubectl
            sudo mv ./kubectl /usr/local/bin/kubectl
            sudo kubectl apply -f ~/project/microservices/eureka-server/k8s-deployment.yaml --kubeconfig ~/project/microservices/eureka-server/kubeconfig --validate=false
      - run:
          name: Deploy to Kubernetes
          command: |
            sudo kubectl set image my-k8s-deployment microservices=alfiodimauro/adm_solutions:eureka_server --kubeconfig ~/project/microservices/eureka-server/kubeconfig
workflows:
  build-and-test:
    jobs:
      - build-eureka-server
      - build-order-service:
          requires:
             - build-eureka-server
      - build-product-service:
          requires:
             - build-eureka-server       
      - build-inventory-service:
          requires:
             - build-eureka-server
      - build-api-gateway:
          requires:
             - build-eureka-server
             - build-order-service
             - build-product-service
             - build-inventory-service
      - deploy:
          requires:
             - build-api-gateway
