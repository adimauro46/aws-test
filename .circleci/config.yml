# This config was automatically generated from your source code
# Stacks detected: deps:java:microservices/product-service,tool:gradle:
version: 2.1
jobs:
  build-eureka-server:
    docker:
      - image: cimg/openjdk:17.0
    working_directory: ~/project/microservices/eureka-server
    steps:
      - checkout:
          path: ~/project
      - run:
          name: Calculate cache key
          command: |-
            find . -name 'pom.xml' -o -name 'gradlew*' -o -name '*.gradle*' | \
                    sort | xargs cat > /tmp/CIRCLECI_CACHE_KEY
      - restore_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
      - run:
          name: set gradle
          command: sudo chmod +x ./gradlew
      - run:
          name: Build
          command: ./gradlew build
      - store_test_results:
          path: build/test-results/eureka-server
      - save_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
          paths:
            - ~/.gradle/caches
      - store_artifacts:
          path: build/reports
  build-order-service:
    docker:
      - image: cimg/openjdk:17.0
    working_directory: ~/project/microservices/order-service
    steps:
      - checkout:
          path: ~/project
      - run:
          name: Calculate cache key
          command: |-
            find . -name 'pom.xml' -o -name 'gradlew*' -o -name '*.gradle*' | \
                    sort | xargs cat > /tmp/CIRCLECI_CACHE_KEY
      - restore_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
      - run:
          name: set gradle
          command: sudo chmod +x ./gradlew
      - run:
          name: Build
          command: ./gradlew build
      - store_test_results:
          path: build/test-results/order-service
      - save_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
          paths:
            - ~/.gradle/caches
      - store_artifacts:
          path: build/reports
  build-product-service:
    docker:
      - image: cimg/openjdk:17.0
    working_directory: ~/project/microservices/product-service
    steps:
      - checkout:
          path: ~/project
      - run:
          name: Calculate cache key
          command: |-
            find . -name 'pom.xml' -o -name 'gradlew*' -o -name '*.gradle*' | \
                    sort | xargs cat > /tmp/CIRCLECI_CACHE_KEY
      - restore_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
      - run:
          name: set gradle
          command: sudo chmod +x ./gradlew
      - run:
          name: Build
          command: ./gradlew build
      - store_test_results:
          path: build/test-results/product-service
      - save_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
          paths:
            - ~/.gradle/caches
      - store_artifacts:
          path: build/reports
  build-inventory-service:
    docker:
      - image: cimg/openjdk:17.0
    working_directory: ~/project/microservices/inventory-service
    steps:
      - checkout:
          path: ~/project
      - run:
          name: Calculate cache key
          command: |-
            find . -name 'pom.xml' -o -name 'gradlew*' -o -name '*.gradle*' | \
                    sort | xargs cat > /tmp/CIRCLECI_CACHE_KEY
      - restore_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
      - run:
          name: set gradle
          command: sudo chmod +x ./gradlew
      - run:
          name: Build
          command: ./gradlew build
      - store_test_results:
          path: build/test-results/inventory-service
      - save_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
          paths:
            - ~/.gradle/caches
      - store_artifacts:
          path: build/reports
  build-api-gateway:
    docker:
      - image: cimg/openjdk:17.0
    working_directory: ~/project/microservices/api-gateway
    steps:
      - checkout:
          path: ~/project
      - run:
          name: Calculate cache key
          command: |-
            find . -name 'pom.xml' -o -name 'gradlew*' -o -name '*.gradle*' | \
                    sort | xargs cat > /tmp/CIRCLECI_CACHE_KEY
      - restore_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
      - run:
          name: set gradle
          command: sudo chmod +x ./gradlew
      - run:
          name: Build
          command: ./gradlew build
      - store_test_results:
          path: build/test-results/api-gateway
      - save_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
          paths:
            - ~/.gradle/caches
      - store_artifacts:
          path: build/reports        
  deploy:
    docker:
      - image: circleci/python:3.8
    steps:
      - checkout
      - setup_remote_docker:
          version: 20.10.7
      - run:
          name: Install kubectl
          command: |
            curl -LO "https://storage.googleapis.com/kubernetes-release/release/$(curl -s https://storage.googleapis.com/kubernetes-release/release/stable.txt)/bin/linux/amd64/kubectl"
            chmod +x ./kubectl
            mv ./kubectl /usr/local/bin/kubectl
      - run:
          name: Deploy to Kubernetes
          command: |
            kubectl set image deployment/my-k8s-deployment my-container=alfiodimauro/my-k8s-app:$CIRCLE_SHA1
workflows:
  build-and-test:
    jobs:
      - build-eureka-server
      - build-order-service
      - build-product-service
      - build-inventory-service
      - build-api-gateway
    #- deploy:
    #     requires:
    #       - build-api-gateway
